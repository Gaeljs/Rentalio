<nav class="navbar navbar-expand-lg bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="{{path('app_main')}}">Rentalio</a>
        {% if app.user %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
            <a class="nav-link text-white" aria-current="page" href="{{path('app_main')}}">Accueil</a>
            </li>
            <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_logements')}}">Logement</a>
            </li>
            <li class="nav-item">
            <a class="nav-link text-white" href="{{path('app_locataires')}}">Locataire</a>
            </li>
            <li class="nav-item">
            <a class="nav-link text-white" href="{{path('app_paiements')}}">Paiement</a>
            </li>
            <li class="nav-item">
            <a class="nav-link text-white" href="{{path('app_etatdeslieux')}}">Etat des lieux</a>
            </li>
            <li class="nav-item">
            <a class="nav-link text-white" href="{{path('app_contrats')}}">Contrat</a>
            </li>
        </ul>
        {% endif %}
        {% if not app.user %}
        <a href="{{path('app_login')}}" class="btn btn-dark mx-2 text-white">Se connecter</a>
        {% endif %}
        {% if app.user %}
        <a href="{{path('app_logout')}}" class="btn btn-dark mx-2 text-white">Deconnexion</a>
        {% endif %}
    </div>
</nav>